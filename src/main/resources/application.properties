# 1. Database Connection (Using your ACTUAL Public Proxy)
spring.datasource.url=jdbc:mysql://shortline.proxy.rlwy.net:15421/railway
spring.datasource.username=root
spring.datasource.password=QvZYVAethSRkhvNbmzMSNjqKzHFbgAdn
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Settings (To prevent "Communications link failure")
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000

# 2. Hibernate Settings
# Batching is crucial for importing large geographical datasets
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Set this to 'update' so it doesn't wipe your manual work,
# but ensures tables exist
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 3. Server Settings
server.port=8081
spring.sql.init.mode=never
spring.jpa.open-in-view=false